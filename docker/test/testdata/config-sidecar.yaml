sidecar:
  server:
    endpoint: :5050
    keep-alive:
      params:
        time: 300s
        timeout: 600s
      enforcement-policy:
        min-time: 60s
        permit-without-stream: false
  monitoring:
    metrics:
      endpoint: :2111

  orderer:
    connection:
      endpoints:
        - :7050
    channel-id: mychannel
    consensus-type: BFT
  committer:
    endpoint: localhost:5002
    ledger-path: /root/sc/ledger
    output-channel-capacity: 20
  bootstrap:
    genesis-block-file-path: /root/config/sc-genesis-block.proto.bin

logging:
  development: true
  enabled: true
  level: INFO
