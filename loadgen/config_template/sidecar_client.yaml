
sidecar-client:
  endpoint: localhost:%d
  coordinator:
    endpoint: localhost:%d
  orderer:
    endpoints:
      - id=1,localhost:%d
      - id=2,localhost:%d
      - id=3,localhost:%d
    channel-id: mychannel
    consensus-type: BFT
    signed-envelopes: false
  broadcast-parallelism: 50
